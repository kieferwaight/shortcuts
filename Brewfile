# macOS dependencies (Homebrew)
# Install: brew bundle install

# Core utilities for cross-platform binding generation
brew "yq"
brew "jq"

# Optional: Key remapping tool for macOS (GUI)
# Alternative: Karabiner-Elements (cask, recommended)
cask "karabiner-elements"

# Optional: If managing configs via git
brew "git"
